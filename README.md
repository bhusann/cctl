# cctl
cctl — Lightweight Clevo P15 performance profile &amp; fan controller. Pure C, zero dependencies beyond libc. Controls CPU power (governor, turbo, EPP, RAPL), fans via direct EC I/O, keyboard RGB, webcam/mic toggle, display refresh rate, and NVIDIA GPU module switching. Dynamically detects hybrid P/E-cores and pins itself to E-cores.
